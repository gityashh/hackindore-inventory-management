<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Navbar</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <nav class="bg-gray-800 p-4">
        <div class="container mx-auto flex justify-between items-center">
            <div class="text-white text-2xl">My Inventory App</div>
            <div class="relative group">
                <button class="text-white px-4 py-2">
                    Inventory
                    <svg class="inline w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
                </button>
                <div class="absolute left-0 hidden group-hover:block bg-white text-gray-800 mt-1 rounded-lg shadow-lg">
                    <% Object.keys(inventoryData).forEach(category => { %>
                        <div class="relative group">
                            <button class="w-full text-left px-4 py-2 hover:bg-gray-100">
                                <%= category %>
                                <svg class="inline w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
                            </button>
                            <div class="absolute left-full top-0 hidden group-hover:block bg-white text-gray-800 mt-1 rounded-lg shadow-lg">
                                <% inventoryData[category].forEach(subCategory => { %>
                                    <a href="#" class="block px-4 py-2 hover:bg-gray-100"><%= subCategory %></a>
                                <% }) %>
                            </div>
                        </div>
                    <% }) %>
                </div>
            </div>
        </div>
    </nav>
</body>
</html>